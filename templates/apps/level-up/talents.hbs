{{!-- Talents --}}
<div class="SD-box">
	<div class="header">
		<label>
			{{localize "SHADOWDARK.class.talents.label"}}
			{{#if canRollBoons}}
				{{localize "SHADOWDARK.apps.level-up.or_boons"}}
			{{/if}}
		</label>
		<span>
			<a
				class="fas fa-table-list"
				data-action="viewTalentTable"
				data-tooltip="{{localize 'SHADOWDARK.class.talents_table.label'}}"
			></a>
			{{#if canRollBoons}}
				<a
					class="fas fa-table-list"
					data-action="viewBoonTable"
					data-tooltip="{{localize 'SHADOWDARK.patron.boons_table.label'}}"
				></a>
			{{/if}}
		</span>
	</div>

	<div class="content">
		{{#if talentGained}}
			{{#unless rolls.talent}}
				<button class="level-up" type="button" data-action="rollTalent">
					{{localize "SHADOWDARK.apps.level-up.roll_talent"}}
				</button>
			{{/unless}}
			{{#if canRollBoons}}
				{{#unless rolls.boon}}
					<button class="level-up" type="button" data-action="rollBoon">
						{{localize "SHADOWDARK.apps.level-up.roll_boon"}}
					</button>
				{{/unless}}
			{{/if}}

			{{#if talentsRolled}}
				<div>
					<fieldset class="items" style="height: 100%;">
						{{#unless talentsChosen}}
							<div style="color: rgba(0, 0, 0, 0.6); text-align: center;">
								<em>{{localize "SHADOWDARK.apps.level-up.dragdrop"}}</em>
							</div>
						{{/unless}}
						{{#each talents}}
							<div style="white-space: nowrap;">
								<a
									class="remove-item fas fa-delete-right"
									data-action="deleteTalent"
									data-index="{{@index}}"
								></a>
								<a
									class="content-link"
									data-link
									data-tooltip="{{this.name}}"
									data-uuid="{{this.uuid}}"
								>
									{{this.name}}
								</a>
							</div>
						{{/each}}
					</fieldset>
				</div>
			{{/if}}
		{{else}}
			{{localize "SHADOWDARK.apps.level-up.notalent"}}
		{{/if}}
	</div>
</div>

{{#if fixedTalentsGained}}
<div class="SD-box">
	<div class="header">
		<label>
			{{localize "SHADOWDARK.class.fixed_talents.label"}}
		</label>
		<span>
		</span>
	</div>

	<div class="content">
		<div>
			<fieldset class="items" style="height: 100%;">
				{{#each fixedTalents}}
					<div style="white-space: nowrap;">
						<a
							class="content-link"
							data-link
							data-tooltip="{{this.name}}"
							data-uuid="{{this.uuid}}"
						>
							{{this.name}}
						</a>
					</div>
				{{/each}}
			</fieldset>
		</div>
	</div>
</div>
{{/if}}
