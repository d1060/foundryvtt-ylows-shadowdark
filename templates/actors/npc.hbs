<section class="{{cssClass}} flexcol" autocomplete="off">
	<header class="SD-header">
		<img
			class="portrait"
			src="{{ actor.img }}"
			data-edit="img"
			data-image-id
			data-action="editImage"
			data-tooltip="{{ actor.name }}"
			height="{{imgHeight}}"
			width="{{imgWidth}}"
		/>
		<div class="shadowdark-logo">
			<span>ShadowDark</span>
		</div>
		<div class="SD-title">
			<input
				name="name"
				type="text"
				value="{{ actor.name }}"
				placeholder="{{localize 'SHADOWDARK.sheet.actor.placeholder.name'}}"
			/>
		</div>
	</header>

	<nav class="SD-nav tabs" aria-roledescription="{{localize "SHEETS.FormNavLabel"}}">
		{{#each tabs as |tab|}}
		<a data-action="tab" data-group="{{tab.group}}" data-tab="{{tab.id}}"
			{{#if tab.cssClass}}class="{{tab.cssClass}}"{{/if}}
			{{#if tab.tooltip}}data-tooltip="{{tab.tooltip}}"{{/if}}>
			{{#if tab.icon}}<i class="{{tab.icon}}" inert></i>{{/if}}
			{{#if tab.label}}<span>{{localize tab.label}}</span>{{/if}}
		</a>
		{{/each}}
	</nav>

	{{!-- <nav class="SD-nav flex0" data-group="primary">
		<a class="navigation-tab active" data-tab="tab-abilities">
			{{localize "SHADOWDARK.sheet.npc.tab.abilities"}}
		</a>
{{#if showSpells}}
		<a class="navigation-tab" data-tab="tab-spells">
			{{localize "SHADOWDARK.sheet.npc.tab.spells"}}
		</a>
{{/if}}
		<a class="navigation-tab" data-tab="tab-description">
			{{localize "SHADOWDARK.sheet.npc.tab.description"}}
		</a>

		<a class="navigation-tab" data-tab="tab-effects">
			{{localize "SHADOWDARK.sheet.item.tab.effects"}}
		</a>
	</nav>

	<section class="SD-content-body flex1">

		{{> actors/npc/abilities }}
{{#if showSpells}}
		{{> actors/npc/spells }}
{{/if}}
		{{> actors/_partials/effects }}
		{{> actors/npc/description }} --}}

	</section>
</section>
